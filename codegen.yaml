
schema: http://localhost:4000/graphql
documents:
  - src/services/graphql/**/*.{graphql,gql}
generates:
  src/graphql/generated/graphql.ts:
    plugins:
      - typescript
      - typescript-operations
      - typed-document-node
